/**
 * Enum with values, which represent type of location restriction for collectible searching query.
 */
export enum Areas {
    RADIUS = "Radius",
    ADMINISTRAVIVE_AREA = "Area",
    REGION = "Region",
    WORLD = "World"
}